/*
* 在本课程中，
* 我们使用state hook来为我们的React组件添加状态，React较新版本(>=16.8.0)就可以使用。
* 在增加钩子之前，没有办法向功能组件添加状态，
* 需要状态的组件必须被定义为class组件，使用JavaScript的类语法。
* state hook作为React当前以及未来的风格固然重要，
* 但许多老项目老代码需要维护，所以还要学习React中 class 类的组件。
*
* 我们很幸运，在调试方面，React是一个对开发者极其友好的库。
* console.log('props value is', props) 和Java不一样。
* 记录到控制台决不是调试我们的应用的唯一方法。
* 您可以在Chrome开发者控制台的调试器中暂停应用代码的执行，
* 方法是在代码的任何地方写下【 debugger 】命令。
*
* 事件属性=事件处理函数的本身或者它的引用，也可以调用“返回函数的函数”。
* */